//pass upward to App.js and coditionally render inside of 
//front page if they are defind show one page if they are not defined show another message

import React from 'react';

export default class RegisterPage extends React.Component {

    render(){
        return(
            <div>
                This is the Register Page
            </div>
        )
    }
}